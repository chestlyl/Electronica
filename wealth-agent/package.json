{
  "name": "wealth-agent",
  "version": "0.1.0",
  "description": "An integrity-first agent that grows a $100 seed through honest research, products, and commerce — proposing every action against a virtual ledger while a human controls all real money.",
  "type": "module",
  "bin": {
    "wealth-agent": "dist/cli.js"
  },
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "cli": "tsx src/cli.ts",
    "demo": "tsx src/demo.ts",
    "test": "tsx src/tests/ledger.test.ts && tsx src/tests/integrity.test.ts && tsx src/tests/opportunities.test.ts"
  },
  "dependencies": {
    "commander": "^12.1.0",
    "dotenv": "^16.4.5",
    "zod": "^3.23.8"
  },
  "devDependencies": {
    "@types/node": "^22.7.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  },
  "engines": {
    "node": ">=20"
  }
}
